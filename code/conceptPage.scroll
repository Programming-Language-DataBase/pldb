importOnly
buildHtml
testStrict

tags All

measures.parsers

replace BASE_URL ..
evalNodeJs
 const path = require("path")
 const filePath = path.resolve(particle.root.filePath)
 const {Tables} = require(path.join(path.dirname(filePath), "..", "Computer.js"))
 Tables.getLanguageTemplate(filePath);
