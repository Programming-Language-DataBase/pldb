replaceNodejs
 const path = require("path")
 const {Tables} = require(path.join(path.dirname(path.resolve(particle.root.filePath)), "..", "Computer.js"))
 module.exports = Tables.allFeaturesImports
tags All
title All Features

header.scroll
printTitle

mediumColumns 1

A list of COUNT features found in programming languages.

endSnippet

TABLE

footer.scroll
tableSearch
